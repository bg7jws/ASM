这是一个386控制台程序，用两个api函数，一个用来读出标准输出的handle
另一个用控制台写字符串向标准输出写指定长度的字符串。
这个程序主要内容是熟悉win32控制台编程的基本步骤和框架
需要注意的是编译连接命令有变化
ml /Zi /c /Fl /coff  %1.asm				/Zi--加符号调试信息   /Fl--产生列表文件
link /subsystem:console %1.obj			/说明是控制台编程
如果仍然使用windows模式的makeit.bat,会生成exe文件，但是运行后无显示直接返回。